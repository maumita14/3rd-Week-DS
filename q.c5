#include <stdio.h>
int main()
  int m,n,j,i, sum=0;
  float main;
  printf "(enter number of rows:");
  scanf("%d",&m);
  printf "(enter number of columns:");
  scanf("%d",&n);
  int arr[m][n];
  printf ("enter elements of the rows:\n")
  for(i=0; i<m;i++){
    for (j=0;j<n;j++){
        scanf("%d",&arr[i][j]);
        sum=arr[i][j];
    }
    mean (float)sum/ (m*n);
    printf ("sum of the elements =%d\n",sum);
    printf ("mean of the elements =%2f\n",mean);
    
    return 0;

    

  }
