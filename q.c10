#include<stdio.h>
int main(){

    int n,i,j;
    long long product=1;

    printf("enter the size of square matrix:");
    scanf("%d",&n);
    int arr[n][n];

    printf("Enter the elements of the %dx%dx matrix:\n",n,n);
      for (i=0;i<n;i++){
        for (j=0;j<N;j++){
            scanf("%d",&arr[i][j]);
        }
      }
      printf("product of elements above main diadonal=%11d\n",product);

    return 0;
}
